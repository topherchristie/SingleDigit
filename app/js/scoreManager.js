define([], function(){
   function ScoreManager(){
   }
   _.extend(ScoreManager.prototype, Backbone.Events);
   
   ScoreManager.prototype.init = function(){
       
   }
   
   //Backbone.sync = function
   
   return ScoreManager;
});